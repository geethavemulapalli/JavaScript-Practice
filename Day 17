1. /* Pre-FizzBuzz Workout #1 */

function preFizz(n) {
var result = [];
for(var i = 1; i <= n; i++){
result.push(i);
}
return result;
}

2. /* Grasshopper - Summation */

var summation = function (num) {

let ans = [...Array(num)].map((x,i) => i + 1);
if(ans){
   return ans.reduce((a,b) => a+b);
}

}


3. /* Printing Array elements with Comma delimiters*/

function printArray(array){
return (array.join(',')).toString();
}

4./* Fun with ES6 Classes #1 - People, people, people */


class Person {
  Person(firstName,lastName,age,gender){
   this.firstName = "John";
   this.lastName = "Doe";
   this.age = 0;
   this.gender = "Male";
  }

   sayFullName(){
   return `${this.firstName} ${this.lastName}`;
  }

   static greetExtraTerrestrials(raceName){
   return `Welcome to Planet Earth ${raceName}`;
  }

}



5./* SpeedCode #3 × Fun with ES6 Classes #5 - Dogs and Classes  */

class Labrador extends Dog {
  constructor(name, age, gender, master) {
    super(name, age, gender, "Labrador", "Large", master, true);
  }
}
